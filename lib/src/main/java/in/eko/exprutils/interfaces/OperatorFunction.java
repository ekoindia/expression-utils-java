package in.eko.exprutils.interfaces;

/**
 * Common interface for all operator functions
 */
public interface OperatorFunction {
    Object apply(Object operand1, Object operand2, Object operand3);
}
